{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            ACTIONS = {changeMonitor = id; configureMonitor = id; freeze = id; refresh = id; }; 
            CLASS = LLMonitorController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                configureButton = NSButton; 
                freezeButton = NSButton; 
                headerField = NSTextField; 
                monitorMenu = NSPopUpButton; 
                refreshButton = NSButton; 
                text = NSTextView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            ACTIONS = {freeze = id; refresh = id; }; 
            CLASS = LLReportController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                freezeButton = NSButton; 
                headerField = NSTextField; 
                refreshButton = NSButton; 
                text = NSTextView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {
            ACTIONS = {refresh = id; }; 
            CLASS = LLSettingsController; 
            LANGUAGE = ObjC; 
            OUTLETS = {headerField = NSTextField; refreshButton = NSButton; text = NSTextView; }; 
            SUPERCLASS = NSWindowController; 
        }
    ); 
    IBVersion = 1; 
}